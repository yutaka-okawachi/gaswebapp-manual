# gaswebapp-manual 作業指示

このファイルは、このリポジトリで作業する Codex へのプロジェクト共通指示です。
ユーザーからの明示的な依頼、上位階層の `AGENTS.md`、システム上の安全規則がある場合は、それらを優先してください。

## プロジェクトの目的

このリポジトリは、ドイツ語の音楽用語検索サイト、管理画面、Google Apps Script（GAS）、および利用者・開発者向けマニュアルを管理します。

主な関心事項は次のとおりです。

- 検索機能と辞書データの正確性
- GAS の安定性、通知の重複抑止、入力検証
- 管理者モードとアクセス計測除外の安全な運用
- GA4・Google Search Console（GSC）の計測と検索状況
- GitHub Pages 上の公開サイトとマニュアルの整合性
- 変更後も既存の検索、同期、通知、公開ページを壊さないこと

なお、管理者ダッシュボードの画面・入力検証・Sites公開は別リポジトリの担当です。本リポジトリでは、主に固定GAS APIとその契約・検証を扱います。

## 作業開始時

1. リポジトリのルート、現在のブランチ、`git status` を確認する。
2. `README.md`、関連する `manuals/` 内の文書、対象コードを先に読む。
3. リポジトリ内にさらに具体的な `AGENTS.md` がある場合は、対象ファイルに最も近いものも適用する。
4. ユーザーの未コミット変更は本人の作業として扱い、上書き・破棄・巻き戻しをしない。
5. 不明なファイル名や関数名を推測せず、`rg` または `rg --files` で確認する。

## 変更の原則

- 依頼された目的を満たす最小限の変更にする。
- 無関係な整形、命名変更、ファイル移動、依存関係更新を同時に行わない。
- 既存の文字コード、改行コード、インデント、文体を尊重する。
- 秘密情報、メールアドレス、API キー、デプロイ ID、個人情報をコードやログへ新たに埋め込まない。
- 入力値や URL パラメータは信頼せず、許可リスト、長さ、型、空文字、異常値を検証する。
- ログや通知には、必要以上の検索語、User-Agent、個人情報を残さない。
- 仕様変更を伴う場合は、コードと同じ作業内で該当マニュアルも更新する。
- 既存の公開 URL やブックマークを壊す変更では、互換性または移行方法を検討する。
- `mahler-search-app/dic.html`、`mahler-search-app/data/`、`src/sync_build.js` などの生成物は直接修正せず、スプレッドシート、GAS、生成スクリプトなどの正本を修正して再生成する。

## このプロジェクトで維持する既定方針

### サイトマップと robots.txt

- `sitemap.xml` は現在の場所を維持する。明示的な依頼なしにルートへ移動、複製、削除しない。
- `robots.txt` とサイトマップの関係を変更するときは、実際の公開 URL と GitHub Pages の配信パスを確認する。
- GSC の取得失敗だけを根拠に、サイト構造を大きく変更しない。

### 管理者モードと計測除外

- 管理者モードの切替は、対象検索ページの URL に付く `?admin=1` と `?admin=0` の現行仕様を前提とする。
- 例：`/mahler-search-app/mahler.html?admin=1`
- ルートの `index.html` に同じパラメータを付ければ全ページへ自動適用される、と仮定しない。
- `admin=1` は端末単位の GA4・検索通知・検索履歴送信オプトアウトであり、認証・認可やアクセス制御ではない。
- オプトアウト済み端末の通常の検索送信経路では、現在の仕様に従い、メール通知と検索履歴への記録を抑止する。これをサーバー側の権限管理や全経路の記録保証とみなさない。
- 管理者モードの判定・解除を意図せず緩めたり、計測除外の範囲を広げたりしない。

### GAS の検索通知

- 同一 payload の短時間重複は、ハッシュキーと `CacheService` を用いる設計を優先する。
- 重複確認とキャッシュ書込みの競合は、必要に応じて `LockService` で保護する。
- 同一 User-Agent だけを理由に、内容の異なる正当な検索まで抑止しない。
- `page` は既知の検索ページだけを許可する。
- `work`、`scope`、`term`、`userAgent` などは型、長さ、許容値を検証する。
- メール送信、スプレッドシート記録、キャッシュ更新の順序と、途中失敗時の挙動を確認する。
- Apps Script の権限スコープを広げる変更は、必要性と影響を説明してから行う。

### データ同期

- `sync-data` の実行条件と対象範囲を、既存スクリプトとマニュアルで確認する。
- 通常の `sync-data`（`-Mode Auto`）は、検証、必要なGAS更新、データ取得、生成物の更新、commit、GitHub push、Pages/API確認までを自動実行する一連の公開処理として扱う。GAS自体はGitHubへ書き込まず、公開はローカルの同期処理が行う。
- `-Mode Data`、`-Mode All`、`-Mode Site`、`-Mode Check`、`-Mode Verify` はそれぞれ既存スクリプトとマニュアルの定義に従う。特に `Check` は本番変更を行わず、`Verify` は新しい版やコミットを作らず、`Site` はGAS未反映変更がある場合に停止する。
- ユーザーが実行するモードを明示して `sync-data` を依頼した場合、そのモードに含まれる commit、push、deploy は承認済みとして扱う。ただし、実行前に対象ブランチ、リモート、未コミット変更、未完了Git操作、同期対象を確認する。
- `-AllowDataRemoval` は辞書データ等の意図した削除についての別個の明示承認なしに付けない。
- `sync-data` 完了後に、同じ変更へ commit、push、deploy を重ねて実行しない。
- 軽微な文書修正だけで、辞書データ同期が必要だと決めつけない。
- データ形式を変更した場合は、生成物だけでなく同期元、検証処理、利用側コードへの影響も確認する。
- 大量更新の前後では差分件数と代表例を確認し、意図しない全件書換えを避ける。

### GA4・GSC

- Applebot などのクローラー記録と GA4 の人間ユーザー計測を混同しない。
- GA4 のイベント名、パラメータ、キーイベント設定を変更するときは、既存レポートとの継続性を考慮する。
- GSC の順位やインデックス状況について、単一日の変動だけで原因を断定しない。
- 計測コード変更後は、通常利用者と管理者モードの双方で送信有無を確認する。

## 実装・検証

変更内容に応じて、利用可能な検証を実行してください。

- 構文検査、lint、既存テスト
- HTML・CSS・JavaScript の参照切れとコンソールエラーの確認
- URL パラメータあり・なし、`admin=1`・`admin=0` の確認
- 正常入力、空入力、長すぎる入力、不正な `page` の確認
- 同一検索の連続送信と、異なる検索の連続送信の確認
- モバイル幅と PC 幅での主要画面確認
- `git diff --check`
- `git diff` で変更範囲と意図しない生成物がないことを確認

テストや実機確認ができない場合は、実行したように扱わず、未確認事項として明記してください。
通常の `sync-data` はブラウザの承認操作や目視確認なしで、自動検証により完了させます。目視確認が必要な場合は、公開処理とは分けて `node scripts/preview-site.js --open` などを実行します。

## Git・公開・外部操作

- ユーザーから明示的に依頼されない限り、`git commit`、`git push`、PR 作成、GAS デプロイ、GitHub Pages の公開操作を行わない。
- ただし、ユーザーから `sync-data` の実行を依頼された場合は、同コマンドに組み込まれた commit、push、deploy も実行対象に含まれる。
- コミットを依頼された場合は、変更内容が一つの目的にまとまるようにする。
- 既存履歴を書き換える操作や、未コミット変更を破棄する操作を行わない。
- 強制 push、`git reset --hard`、広範囲の削除は行わない。
- 外部サービスの設定変更前に、対象アカウント、プロパティ、サイト、デプロイ先を確認する。

## マニュアル更新

- 利用者向け説明と開発者向け説明を区別する。
- 操作手順では、対象ページ、入力する URL、期待される結果を具体的に書く。
- 現在の画面や実装に存在しないボタン、メニュー、機能を推測で記載しない。
- 同じ仕様を説明する複数文書がある場合は、矛盾が生じないよう関連箇所を検索する。
- Windows の利用者が実行するコマンドは、PowerShell でそのまま使えるか確認する。

## 完了報告

作業完了時は簡潔に、次の事項を報告してください。

1. 何を変更したか
2. どのファイルを変更したか
3. どの検証を実行し、結果がどうだったか
4. 未確認事項または利用者側で必要な操作
5. commit、push、deploy を行ったかどうか

問題を見つけても、依頼範囲外の修正は勝手に広げず、影響と推奨対応を報告してください。
